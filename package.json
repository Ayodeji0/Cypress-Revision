{
  "name": "cypressrevision",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run",
    "headTest": "npx cypress run --headed",
    "chromeTest": "npx cypress run --browser chrome",
    "smokeTest": "npx cypress run --spec \"cypress/e2e/test1.cy.js\""
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^15.2.0",
    "cypress-mochawesome-reporter": "^4.0.2",
    "neat-csv": "5.1.0"
  },
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^23.2.0"
  }
}
